# Normalize all text files to LF in the repo and working tree.
# Avoids CRLF churn on Windows checkouts (autocrlf) and matches upstream.
* text=auto eol=lf

# Windows-only scripts keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
